<!DOCTYPE html>
<html>
  <head>
    <title>ながのcakeオレンジ</title>
  </head>

  <body>
    <div class="container">
      <div class="row">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-group">
            <%= f.label "メールアドレス" , class: 'col-sm-2 control-label' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email" , class: 'col-xs-5' %>
          </div>
      </div>

        <div class="row">
          <div class="form-group">
            <%= f.label "パスワード" , class: 'col-sm-2 control-label' %>
            <%= f.password_field :password, autocomplete: "current-password" , class: 'col-xs-5' %>
          </div>
        </div>

        <div class="row">
          <div class="form-group">
            <%= f.submit "ログイン" , class: 'btn btn-primary col-sm-offset-3 col-xs-3' %>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
