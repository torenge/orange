<!DOCTYPE html>
<html>
<head>
	<title>ながのcakeオレンジ</title>
</head>
<body>
	<div class="body">
		<div class="container">
			<div class="row">
				<h1>購入情報入力</h1>
				<%= form_for(@order, url: public_order_product_path(@order)) do |f| %>
					<h2>支払方法</h2>
						<P><%= f.radio_button :pay_method, "1" %>クレジットカード</P>
						<p><%= f.radio_button :pay_method, "2" %>銀行振込</p>
					<h2>お届け先</h2>
						<P>
							<%= f.radio_button :address_choice, "1" %>
							ご自身の住所<br>
							〒<%= @order.user.postal_code %><br>
							<%= @order.user.address %><br>
							<%= @order.user.first_name %>
							<%= @order.user.last_name %>
						</P>

						<p>
							<%= f.radio_button :address_choice, "2" %>
							登録済住所から選択
								<%= f.collection_select :deli_address_id, @user.deli_addresses, :id, :address %>
						</p>

						<p>
							<%= f.radio_button :address_choice, "3" %>
							新しいお届け先

								<label>郵便番号</label>ハイフンなし
								<%= f.text_field :postal_code %>
								<label>住所</label>
								<%= f.text_field :address %>
								<label>宛名</label>
								<%= f.text_field :first_name %>
								<%= f.text_field :last_name %>

						</p>

					<%= f.submit "確認画面へ進む" %>
				<% end %>
			</div>
		</div>
	</div>
</body>
</html>